export interface UserData {
    fullName: string;
    email: string;
    dateOfBirth?: string; // Campo opzionale
    province?: string; // Campo opzionale
    city?: string; // Campo opzionale
    address?: string; // Campo opzionale
    zipCode?: string; // Campo opzionale
    phoneNumber?: string; // Campo opzionale
    bio?: string; // Campo opzionale
    imageUrl?: string; // Campo opzionale
}